[user: id (string)] => {
  name: tag (string) # user tag, will be updated each time a command is used
  cycles: big # will be used from big.js
  cpp: big # cycles per post
  cpm: big # cycles per minute
  langs: string[] # languages the user knows (boost)
  level: big # level boost
  youtube: big # youtube boost
  inv: item[]
  bought: {
    
  }
}
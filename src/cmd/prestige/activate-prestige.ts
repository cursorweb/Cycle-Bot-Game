import * as Discord from "discord.js";
import { Command, Colors, Bot, brackets } from "../../global";


class C extends Command {
  names = ["use-prestige"];
  help = "Prestige, (In progress)."
  isGame = 'n' as 'n';
}